<?xml version="1.0" encoding="utf-8"?><!-- The android:opacity=”opaque” line — this is critical in preventing a flash of black as your theme transitions. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" android:opacity="opaque">    <!-- The background color, preferably the same as your normal theme -->
    <item android:drawable="@color/green"/>
    <!-- Your splash image -->
    <item>
        <bitmap
            android:src="@drawable/splash_screen_background"
            android:tileMode="repeat"/>
    </item>
    <item
        android:height="38dp"
        android:width="200dp"
        android:drawable="@drawable/example_logo"
        android:gravity="center" />
</layer-list>