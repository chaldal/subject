namespace LibClient.Chars

// NOTE add entities as you find use for them. I considered importing
// some large list of entities, but couldn't find one that just has
// "useful" entities, most such lists tend to be complete, and thus
// have hundreds of obscure, useless entities.
module Char =
    let nbsp       = "\u00A0"
    let raquo      = "\u00BB"
    let laquo      = "\u00AB"
    let percent    = "%"
    let braceOpen  = "{"
    let braceClose = "}"
    let apostrophe = "'"
    let rarrow     = "\u2192"
