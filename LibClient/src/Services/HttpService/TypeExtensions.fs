module LibClient.Services.HttpService.TypeExtensions

// This is getting out of hand now that we're allowing type extensions on stuff
// other than components (basically any JS interop).
// Need to just have one top level TypeExtensions file per project,
// but I don't want to deal with the work to convert right now.


// This type gets implicitly extended externally
type TEs =
    class end
